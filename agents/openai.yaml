interface:
  display_name: "Unreal MCP"
  short_description: "Automate Unreal Editor tasks through official MCP."
  default_prompt: "Use $unreal-mcp to inspect and configure the UE project, recover the MCP connection, execute the requested editor task, and independently verify the result; request only a minimal blocker when automation cannot continue safely."

# Codex MCP config (.codex/config.toml):
# [mcp_servers.unreal-mcp]
# url = "http://127.0.0.1:8000/mcp"
#
# Generate with: ModelContextProtocol.GenerateClientConfig Codex
